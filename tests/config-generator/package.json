{
  "name": "tuic-config-generator-browser-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Browser regression tests for the standalone Svelte/WASM configuration generator",
  "devDependencies": {
    "playwright": "1.62.1"
  },
  "engines": {
    "node": ">=20"
  }
}
